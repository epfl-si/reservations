apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: reservations
  name: reservations-route
  namespace: reservations-test
spec:
  host: reservations-preprod.epfl.ch
  port:
    targetPort: 8080-tcp
  tls:
    termination: edge
  to:
    kind: Service
    name: reservations-service
    weight: 100
  wildcardPolicy: None